{
  "name": "location-share-backend-fresh",
  "version": "1.0.0",
  "description": "Fresh backend for location sharing",
  "main": "api/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {}
}
